# Decision Log (ADR-lite)

Use `adr-template.md` for each entry. Append new decisions below; never edit history, only
supersede.

## Open ADRs (block implementation until resolved)
- Vector DB choice: Qdrant assumed per PRD §4 recommendation — confirm before first RAG-dependent
  task starts.
- LLM router policy: exact fast/heavy model split and caching strategy (PRD §3.1).
- Document diff/versioning model: how chapter edits are represented for the diff viewer.
- Billing ledger schema: `User`/`Wallet`/`Transaction` shape (PRD §5).

## Decisions
_(none recorded yet)_
